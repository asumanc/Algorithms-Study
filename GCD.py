def gcd (a,b)
	if b==0:
	return a:

	else return gcd (b,a mod b):

-
-
-
-
#Other way:

def gcd (a,b)

	if ((a%b)==0):
		return b
	else 
		return gcd (b,a%b)
		pass

#Other Way
def gcd(a,b)*lcm(a,b)=ab

	int lcm (int a, int b);

return (a*b)/GCD-recursion (a,b)
